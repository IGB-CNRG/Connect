IGB Connect Weekly Digest

The following entries and exits have occurred this week:

Entries

{%~ apply format_text_table %}
Name	UIN	Start Date	Themes
{%~ for entry in entries %}
{{ entry.person }}	{{ entry.person.uin }}	{{ entry.date|date('n/j/Y') }}	{{ entry.notes }}
{% endfor ~%}
{% endapply ~%}

Exits

{%~ apply format_text_table %}
Name	UIN	Start Date	Themes	Exit Reason
{%~ for exit in exits %}
{{ exit.person }}	{{ exit.person.uin }}	{{ exit.date|date('n/j/Y') }}	{{ exit.notes }}	{% if exit.person.exitForm is not null %}{{ exit.person.exitForm.exitReason }}{% endif %}

{% endfor ~%}
{% endapply ~%}
